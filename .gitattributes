# .gitattributes - 줄바꿈 자동 처리

# 기본: 자동 감지
* text=auto

# Windows 전용 파일 (CRLF 유지)
*.sln text eol=crlf
*.csproj text eol=crlf
*.cs text eol=crlf
*.xaml text eol=crlf

# 스크립트 (LF 유지 - Linux에서도 실행 가능)
*.sh text eol=lf

# 바이너리 파일 (변환 안 함)
*.png binary
*.ico binary
*.dll binary
*.exe binary